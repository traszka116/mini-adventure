// #include "game/camera.h"

// vec2_t camera_coords(camera_t camera, vec2_t vec)
// {
//     vec2_t result;
//     result.x = (vec.x - camera.position.x + camera.size.x / 2);
//     result.y = (vec.y - camera.position.y + camera.size.y / 2);
    
//     return result;
// }